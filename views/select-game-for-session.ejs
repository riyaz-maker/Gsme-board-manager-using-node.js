<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Game for Session</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        body {
            background-image: url('/images/background.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            color: #fff;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .container {
            max-width: 600px;
            margin: 50px auto;
            background: rgba(0, 0, 0, 0.8);
            color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        .game-list {
            list-style: none;
            padding: 0;
        }

        .game-item {
            background: rgba(255, 255, 255, 0.1);
            margin: 10px 0;
            padding: 10px 15px;
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .game-item a {
            color: #ff5722;
            text-decoration: none;
            font-weight: bold;
            padding: 5px 10px;
            background-color: #333;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

        .game-item a:hover {
            background-color: #ff5722;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Select a Game</h1>
        <ul class="game-list">
            <% games.forEach(game => { %>
                <li class="game-item">
                    <span><%= game.Name %></span>
                    <a href="/sessions/add/<%= game.GameID %>">Add Session</a>
                </li>
            <% }) %>
        </ul>
    </div>
</body>
</html>
